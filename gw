#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

console_mode="plain"
if [[ -t 1 ]]; then
  console_mode="rich"
fi

exec "$script_dir/gradlew" --console="$console_mode" "$@"
